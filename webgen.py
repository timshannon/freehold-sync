#!/usr/bin/env python
import sys
import subprocess
import os
import shutil

#Generates runs r.js optimizer to package an entire webpage into one html file
# then embeds that html file in a .go file as a string const for building
